#ifndef APPLY_DEF
#define APPLY_DEF

#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include "my_tab.h"
#include "index.h"

/*
 * Execute une commande apres traitement
 */
int apply(my_tab t);

#endif
